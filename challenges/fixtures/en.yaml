# LLMOrchestrator round-275 challenge fixture — English (en).
#
# Drives the Challenge runner at challenges/runner/main.go through a
# real parser + protocol + i18n flow. Anti-bluff: the message id MUST
# round-trip verbatim through NoopTranslator (CONST-035 surface — a
# silent string substitution would mask the gap a project-injected
# translator must fill).
locale: en
prompt_json: '{"actions":[{"type":"click","target":"login","confidence":0.95}]}'
expect_action_type: click
expect_action_target: login
expect_pipe_content: "tap the login button"
expect_message_id: llmorch_intent_click_login
