# LLMOrchestrator round-291 anti-bluff fixture — locale: en
# Each fixture pins a real ResponseParser action extraction, a real
# PipeMessage round-trip Content, and a real FileTransport inbox ID.
# All three values are compared verbatim by challenges/runner/main.go
# — drift in any field fails the gate.
locale: en
prompt_json: '{"actions":[{"type":"click","target":"login_button","confidence":0.92}]}'
expect_action_type: click
expect_action_target: login_button
expect_pipe_content: round-291 en pipe payload
expect_inbox_id: round-291-en
